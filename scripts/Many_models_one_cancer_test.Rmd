---
title: "Many Elastic Net Models for `r params$cancer_type` across layers"
author: "Monti Lab"
date:   "`r format(Sys.Date(), '%B %d, %Y')`"
params: 
  cancer_type: "HNSC"
output:
  html_document:
    theme: united
    code_folding: hide
    css: style.css
    toc: true
    toc_float: true
---

```{r opts, echo=FALSE}
#theme: united
## to prevent excessively verbose output
knitr::opts_chunk$set(echo = TRUE, message=FALSE, warning=FALSE)
```

```{r render_report, echo=FALSE}
## this is needed only if you want to generate different versions of the output html
#render_report <- function(
#    rmd = rstudioapi::getSourceEditorContext()$path, # the current source file
#    cancer_type ) {
  #cancer_type <- match.arg(cancer_type)
#  rmarkdown::render(
#    rmd,
#    params = list(cancer_type = cancer_type),
#    output_file = paste(tools::file_path_sans_ext(rmd), cancer_type, "html", sep = ".")
#  )
#}
# E.g.,
#
#render_report(cancer_type = "HNSC") 
#   --> "/path/<rmarkdown_stub>.hnsc.html"
# render_report(cancer_type = "brca") 
#   --> "/path/<rmarkdown_stub>.brca.html"
```

```{r cancer type load, include=FALSE}
library(knitr)
library(glue)
library(reactable)

cancer_type <- params$cancer_type

library(e1071) 
library(caTools) 
library(class)
#library(TimeSeriesExperiment)
#library(SummarizedExperiment)
#library(Biobase)
#library(ggplot2)
#library(caret)
# source("InitialSteps.Rmd")
knitr::knit("ModelBuilding.Rmd", output = tempfile())
knitr::knit("Analysis.Rmd", output = tempfile())
source("PanClockHelperFunctions.R")
setwd("/restricted/projectnb/agedisease/projects/pancancer_aging_clocks/scripts/GitCore/scripts")

```

# Get the output of the ML model (loop through all layer combinations)
```{r Setting Model Combinations}
# Personalized Layer Combinations

#layer_combinations <- list(c("miRNA"),c("RPPA"))
#layer_combinations <- list(c("miRNA"))
#layer_combinations <- list(c("miRNA"),c("miRNA","RPPA"))
#layer_combinations <- list(c("miRNA"),c("RPPA"),c("RNASeq"),c("miRNA","RPPA"),c("miRNA","RNASeq"),c("RNASeq","RPPA"), c("miRNA","RPPA","RNAseq")) #w/o methylation
#layer_combinations <- list(c("methylation"),c("miRNA"),c("RPPA"),c("RNASeq"),c("miRNA","RPPA"),c("miRNA","RNASeq"),c("RNASeq","RPPA"), c("miRNA","RPPA","RNAseq"))

layer_combinations <- list(c("RPPA"))

for (comb in layer_combinations){
  print(comb)
}

#Given a series of models, perform all possible combinations

#models <- c("miRNA", "RPPA", "RNASeq")
#layer_combinations <- unlist(lapply(1:length(models), function(x) {
#  combn(models, x, simplify = FALSE)
#}), recursive = FALSE)

```
# Running Elastic Net Models for: {.tabset}
```{r, Running Models, results = 'asis'}
#Initializing a coefficient empty list so to append after every run
coef_list <- list()

cancer_summary_table <- data.frame(
  test_name = character(),
  cancer_type = numeric(),
  layer_combination = numeric(),
  Rsquared = modelOutput$ModelBuilding$model$R2,
  RMSE = modelOutput$ModelBuilding$model$RMSE,
  delta_age_coeff	= numeric(),
  delta_age_exp_coeff = numeric(),
  delta_age_pval = numeric(),
  chronological_coef = numeric(),
  chronological_exp_coef =numeric(),
  chronological_pval = numeric(),
  
  Concordance_ni = numeric(),
  se_concordance_ni = numeric(),
  lik_ratio_test_ni = numeric(),
  lik_ratio_test_df_ni = numeric(),
  lik_ratio_test_pval_ni = numeric(),
  Wald_test_ni = numeric(),
  Wald_test_df_ni = numeric(),
  Wald_test_pval_ni = numeric(),
  logrank_score_test_ni = numeric(),
  logrank_score_test_df_ni = numeric(),
  logrank_score_test_pval_ni = numeric(),
  
  Concordance_i = numeric()
  se_concordance_i = numeric()
  lik_ratio_test_i = numeric(),
  lik_ratio_test_df_i = numeric(),
  lik_ratio_test_pval_i = numeric(),
  Wald_test_i = numeric(),
  Wald_test_df_i = numeric(),
  Wald_test_pval_i = numeric(),
  logrank_score_test_i = numeric(),
  logrank_score_test_df_i = numeric(),
  logrank_score_test_pval_i = numeric()
  )
  

# Iterate through all values in layer_combinations
for (comb in layer_combinations) {
  # Generate tab heading based on the combination
  #cat(paste("Elastic Net model for", comb, "\n\n"))
  #combination <- comb
  cat("\n\n##", comb, "\n\n")

  # Run standardized model building
  existingExperimentList <- NULL
  modelOutput <- standardizedModelBuilding(comb,
                                           cancerName = cancer_type,
                                           methodNames = c("glmnet"),
                                           splitSize = 0.99,
                                           seed = 43)

  # Extract lambda values and coefficients
  lambda_values <- modelOutput$ModelBuilding$model$finalModel$lambda
  closest_lambda_index <- which.min(abs(lambda_values - modelOutput$ModelBuilding$model$finalModel$lambdaOpt))
  beta_matrix <- modelOutput$ModelBuilding$model$finalModel$beta
  coef_names <- rownames(beta_matrix)
  beta_column <- beta_matrix[, closest_lambda_index]

  coef_df <- data.frame(
    Feature = coef_names,
    Weight = as.numeric(beta_column)
  )
  rownames(coef_df) <- coef_df$Feature

  # Separate non-zero and zero coefficients
  non_zero_coef_df <- coef_df[coef_df$Weight != 0, ]
  zero_coef_df <- coef_df[coef_df$Weight == 0, ]

  # Print summary statistics for non-zero coefficients
  cat(paste("\n\n There are", nrow(non_zero_coef_df), "features with non-zero weights in this model.\n\n"))
  cat(paste("There are", nrow(zero_coef_df), "features with zero weight in this model.\n\n"))

  # Save coef_df to the list with a unique name based on combination
  coef_list[[paste0(cancer_type, "_", comb)]] <- coef_df

  # Calculate and display survival statistics
  cat("\n\n### Evaluating delta age on Survival(coxph)\n\n")
  stats <- getSurvivalStats(modelOutput$experimentList[[1]],
                            modelOutput$ModelBuilding$predicted)
  
  cat("\n\n#### Non Interaction Coxph Model\n\n")
  cox_summary_ni <- capture.output(stats$non_interaction_summary)
  cat("```\n", paste(cox_summary_ni, collapse = "\n"), "\n```\n")
  
  cat("\n\n#### Interaction Coxph Model\n\n")
  cox_summary_i <- capture.output(stats$interaction_summary)
  cat("```\n", paste(cox_summary_i, collapse = "\n"), "\n```\n")
  
  #Making summary tables df1 <- (model performance & coxph) & df2 <- (coefficients and model weight)
  summ_table <- rbind(non_interaction_table
    
  )
  
  # non_interaction_table <- rbind(non_interaction_table,
  #                                  data.frame(
  #                                    test_name = paste0(df_name, "_non_interaction"),
  #                                    coef = non_interaction_summary$coefficients["delta_age", "coef"],
  #                                    exp_coef = exp(non_interaction_summary$coefficients["delta_age", "coef"]),
  #                                    se = non_interaction_summary$coefficients["delta_age", "se(coef)"],
  #                                    concordance = non_interaction_summary$concordance[1],
  #                                    likelihood_ratio_stat = non_interaction_summary$logtest["test"],
  #                                    likelihood_ratio_df = non_interaction_summary$logtest["df"],
  #                                    likelihood_ratio_p = non_interaction_summary$logtest["pvalue"],
  #                                    p_value_delta_age = non_interaction_summary$coefficients["delta_age", "Pr(>|z|)"]
  #                                 )
  #   )
  
  # Print the summary as a Markdown-formatted code block
  #cat("\n\n### Cox Proportional Hazards Model Summary\n\n")
  #cat("```\n", paste(cox_summary, collapse = "\n"), "\n```\n")
  
}

packageVersion("survival") #3.6.4

cancer_type <- 
layer_combination <- 
Rsquared <- modelOutput$ModelBuilding$model$R2
RMSE <- modelOutput$ModelBuilding$model$RMSE
delta_age_coeff	<-
delta_age_exp_coeff <-
delta_age_pval <-
chronological_coef <-
chronological_exp_coef <-
chronological_pval <-

Concordance_ni <- 
se_concordance_ni <-
lik_ratio_test_ni <- 
lik_ratio_test_df_ni
lik_ratio_test_pval_ni
Wald_test_ni
Wald_test_df_ni
Wald_test_pval_ni
logrank_score_test_ni
logrank_score_test_df_ni
logrank_score_test_pval_ni

Concordance_i <- 
se_concordance_i <-
lik_ratio_test_i <- 
lik_ratio_test_df_i <- 
lik_ratio_test_pval_i <- 
Wald_test_i <-
Wald_test_df_i <- 
Wald_test_pval_i <-
logrank_score_test_i <- 
logrank_score_test_df_i <- 
logrank_score_test_pval_i <-

stats$non_interaction_model$coefficients

#Extracting Model Parameters
best_lambda <- modelOutput$ModelBuilding$model$bestTune$lambda
best_alpha <- modelOutput$ModelBuilding$model$bestTune$alpha
modelOutput$ModelBuilding$model$finalModel$
#RMSE
RMSE <- modelOutput$ModelBuilding$model$results$RMSE[
  modelOutput$ModelBuilding$model$results$lambda == best_lambda & 
    modelOutput$ModelBuilding$model$results$alpha == best_alpha]
Rsquared <- modelOutput$ModelBuilding$model$results$Rsquared[
  modelOutput$ModelBuilding$model$results$lambda == best_lambda & 
    modelOutput$ModelBuilding$model$results$alpha == best_alpha]
RMSE #11.42457
Rsquared #0.2691437
#13.02704 12.08236 11.70048 11.50940 11.42457
modelOutput$ModelBuilding$model$results
#Rsquared: 0.446027431754077 - RMSE: 10.1390270582199

```
## Model Weights
```{r Printing Model Weights, results='asis'}
#names(coef_list)

#searchable = true

cat("\n\n# Model Weights\n\n")

library(htmltools) 
  
# Create a list of HTML tags, each containing a header + reactable
all_tables <- lapply(names(coef_list), function(placeholder) {
  div(
    h3(placeholder),
    reactable(coef_list[[placeholder]], searchable = TRUE)
  )
})

# Wrap them all in tagList so they render one after another
tagList(all_tables)
```

## Session Info
```{r session info}
sessionInfo()
```